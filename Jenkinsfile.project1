pipeline {
    agent any
    tools{
        nodejs 'Nodejs'
    }
    stages{
        stage('Teste'){
            steps {
                sh 'cd project1 && npm install && npm run test'
            }
        }
    }
}